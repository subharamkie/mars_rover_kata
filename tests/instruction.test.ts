import { describe } from "node:test";

describe("Instructions functionality tests", () => {
  it("test if instruction", () => {
    const string = "";
    const instructionArray = string.split("");
  });
});
